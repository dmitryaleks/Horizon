# Context

We are developing a project in Python on Windows.

# Rules

Use Windows command line to run code and scripts.

Save temporary scripts into gluecode sub-directory for a reuse, maintain a GLUECODE.md registry of those tools.